name: Deploy to VPS

on:
  workflow_run:
    workflows: ["Playwright Tests"]
    branches: [main]
    types: [completed]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # Pull latest as500-docs code
            cd /var/www/as500-docs
            git pull

            # Pull latest AS500 code and rebuild app
            cd /var/www/AS500
            git pull
            docker compose -f docker-compose.prod.yml build app docs
            docker compose -f docker-compose.prod.yml up -d app docs
